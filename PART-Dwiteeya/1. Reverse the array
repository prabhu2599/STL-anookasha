#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

void printu(vector<int> v) {
    for(int i: v) {
        cout<<i<<" ";
    }
}

int main() {
    vector<int> v = {1,2,3,4,5};
    reverse(v.begin(),v.end());
    printu(v);
    return 0;
}
